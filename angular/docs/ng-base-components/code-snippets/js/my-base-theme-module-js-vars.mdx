```ts
import { Theme } from "@my-org/my-scope.theme.my-theme-provider";

// Design tokens as JS variables
export const myTheme: Theme = {
  myFontSize: "16px",
  myFontFamily: "Roboto, sans-serif",
  myBorderRadius: "4px"
};
```
