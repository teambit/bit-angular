import { AngularAppOptions } from '@teambit/angular';

export const DemoAppOptions: AngularAppOptions = {
  /**
   * Name of the app in Bit CLI.
   */
  name: 'demo-app'
};

export default DemoAppOptions;
